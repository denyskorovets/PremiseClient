import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  grid: {
    marginTop: "40vh"
  },
  progressBar: {
    color: "rgb(229, 9, 20)"
  }
}));
